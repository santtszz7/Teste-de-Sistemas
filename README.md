# Teste de Sistemas

Repositório de projetos da disciplina Teste de Sistemas.

## Projetos

- **[CalculadoraTest](./CalculadoraTest)** — Calculadora em HTML, CSS e JavaScript com testes automatizados (Jest).  
  Estrutura: `src/` (código), `tests/` (testes). Para rodar os testes: `cd CalculadoraTest && npm install && npm test`.
